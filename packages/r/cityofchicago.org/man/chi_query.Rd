% Generated by build script
\name{chi_query}
\alias{chi_query}
\title{Query a Socrata dataset on Chicago Open Data}
\usage{
chi_query()
}
\arguments{
\item{dataset_id}{Socrata dataset identifier (e.g. "ijzp-q8t2")}
\item{where}{Optional SoQL WHERE clause (e.g. "primary_type='THEFT'")}
\item{select}{Optional SoQL SELECT clause (e.g. "date, primary_type")}
\item{order}{Optional SoQL ORDER BY clause (e.g. "date DESC")}
\item{limit}{Number of rows to return (default 1000, max 50000)}
\item{offset}{Offset for pagination (default 0)}
}
\value{tibble with columns from the dataset}
\description{Query a Socrata dataset on Chicago Open Data}
