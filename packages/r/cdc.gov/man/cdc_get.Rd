% Generated by build script
\name{cdc_get}
\alias{cdc_get}
\title{Fetch data from any CDC Socrata dataset}
\usage{
cdc_get()
}
\arguments{
\item{dataset_id}{Socrata dataset ID (e.g. "vbim-akqf" for COVID cases)}
\item{where}{SoQL WHERE clause (e.g. "sex='Female' AND age_group='50-59 Years'")}
\item{select}{SoQL SELECT clause (e.g. "sex, count(*) as n")}
\item{group}{SoQL GROUP BY clause (e.g. "sex")}
\item{order}{SoQL ORDER BY clause (e.g. "count DESC")}
\item{token}{Optional Socrata app token}
\item{max_results}{Max rows (default 1000)}
}
\value{tibble with all columns as character (Socrata returns strings)}
\description{Fetch data from any CDC Socrata dataset}
