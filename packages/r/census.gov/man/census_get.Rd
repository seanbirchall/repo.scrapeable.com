% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/census.R
\name{census_get}
\alias{census_get}
\title{Fetch data from any Census API endpoint}
\usage{
census_get(
  dataset,
  year,
  variables,
  for_geo,
  in_geo = NULL,
  key = NULL,
  label = FALSE
)
}
\arguments{
\item{dataset}{API dataset path (e.g. "acs/acs5", "dec/pl")}

\item{year}{Integer year}

\item{variables}{Character vector of variable names, or "group(B01001)"}

\item{for_geo}{Geography spec (e.g. "state:\emph{", "county:}").
Or pass a list from census_geo_*() helpers.}

\item{in_geo}{Parent geography constraint. String with space-separated
components (e.g. "state:06 county:037") or NULL.}

\item{key}{Optional API key}

\item{label}{If TRUE, join variable labels from the variable catalog (cached)}
}
\value{
tibble with one row per geography, all columns character.
}
\description{
The universal engine. Builds the URL, fetches, and parses the
array-of-arrays JSON into a tibble. All columns return as character —
Census uses "-", "N", "(X)" for suppressed data, so auto-typing is
unsafe. Use mutate(across(..., as.numeric)) on the columns you need.
}
\details{
Automatically chunks requests exceeding the Census API's 50-variable
limit and merges the results.
}
